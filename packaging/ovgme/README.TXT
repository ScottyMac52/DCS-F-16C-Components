DCS F-16C COMPONENTS — OVGME PACKAGE
=====================================

OVGME PACKAGE VERSION {{VERSION}}
Target:  DCS Saved Games directory

This package installs Scott's Thrustmaster Cougar MFD 1 and MFD 2 profiles to:

  Config\Input\F-16C_50\joystick
  KNEEBOARD\F-16C_50

MFD 1 is the left MFD. MFD 2 is the right MFD. MFD 3 is not modified.

Before enabling the package, back up:

  Saved Games\DCS.openbeta\Config\Input\F-16C_50

If your Saved Games installation uses DCS instead of DCS.openbeta, use that root.

Disable the package in OvGME to remove the managed profiles.

OpenKneeboard should discover KNEEBOARD\F-16C_50 through its DCS Aircraft
tab. If not, add that directory as a Folder tab.
